defaults:
  - data: transformer
  - model: transformer
  - training: transformer
  - _self_

hydra:  
  output_subdir: null  
  run:  
    dir: .  


global_args:
  ngpus: 1
  dtype: float32
  savedir: checkpoints
  seed: 42
